<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.majorlink.lab.mapper.SupplyCategoryMapper">

    <select id="selectAll" resultType="SupplyCategory">
        SELECT * FROM supply_category ORDER BY sort ASC, id ASC
    </select>

    <select id="selectById" resultType="SupplyCategory">
        SELECT * FROM supply_category WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supply_category (parent_id, name, code, sort, remark)
        VALUES (#{parentId}, #{name}, #{code}, IFNULL(#{sort}, 0), #{remark})
    </insert>

</mapper>
