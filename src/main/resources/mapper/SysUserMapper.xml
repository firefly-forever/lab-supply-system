<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.majorlink.lab.mapper.SysUserMapper">

    <!-- 结果映射：数据库字段 -> Java字段（因为开启了驼峰转换，基本字段可自动映射） -->
    <resultMap id="BaseResultMap" type="SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="role" property="role"/>
        <result column="department" property="department"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基础查询列（注意：不返回password，防止密码泄露；特殊场景单独处理） -->
    <sql id="BaseColumns">
        id, username, real_name, role, department, phone, email, status, create_time, update_time
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        <!-- 登录时需要查密码，单独列出 -->
        SELECT id, username, password, real_name, role, department, phone, email, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_user
        ORDER BY create_time DESC
    </select>

    <select id="selectByRole" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_user
        WHERE role = #{role} AND status = 1
        ORDER BY real_name
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, role, department, phone, email, status)
        VALUES (#{username}, #{password}, #{realName}, #{role}, #{department}, #{phone}, #{email},
                IFNULL(#{status}, 1))
    </insert>

    <update id="update">
        UPDATE sys_user
        SET real_name   = #{realName},
            department  = #{department},
            phone       = #{phone},
            email       = #{email}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
