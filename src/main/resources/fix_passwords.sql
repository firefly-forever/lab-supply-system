-- ============================================================
-- 密码修复脚本
-- 说明：BCrypt的hash值含随机salt，无法在SQL中静态写死后直接使用。
--      启动项目后，运行 PasswordUtil.main() 生成正确的hash值，
--      然后将生成的hash替换下方的 {HASH_FROM_PASSWORDUTIL}，执行此SQL。
--
-- 快捷方法：项目启动时控制台会打印正确的UPDATE语句，直接复制执行即可。
-- ============================================================

USE lab_supply_db;

-- 将 {HASH} 替换为 PasswordUtil.main() 输出的哈希值
-- 示例（实际hash每次不同，以工具生成为准）：
-- UPDATE sys_user SET password='$2a$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx' WHERE 1=1;

-- ============================================================
-- 如果不想手动生成，也可以用以下方式：
-- 1. 启动项目（会打印正确的UPDATE语句到控制台）
-- 2. 复制控制台输出的UPDATE语句到此处执行
-- ============================================================
